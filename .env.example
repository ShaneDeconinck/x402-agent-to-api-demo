# Anthropic API Key (required)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# API Configuration
API_BASE_URL=http://localhost:8000

# Base Sepolia Configuration
# Public RPC (slow to index, ~20s lag):
# BASE_SEPOLIA_RPC=https://sepolia.base.org
#
# Alchemy RPC (recommended - fast indexing, ~2s lag):
# 1. Sign up: https://www.alchemy.com/
# 2. Create app: Node API → Base → Base Sepolia
# 3. Copy your RPC URL and paste below:
BASE_SEPOLIA_RPC=https://base-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_API_KEY
#
# USDC contract on Base Sepolia (don't change this)
USDC_CONTRACT_ADDRESS=0x036CbD53842c5426634e7929541eC2318f3dCF7e

# Agent Wallet (the agent creates USDC payments from this wallet)
# This is the "AI company" wallet that pays per query
# Export private key from MetaMask: Account Details → Show Private Key
# Copy exactly as shown (64 hex characters, no 0x prefix)
# Fund this wallet with testnet USDC (e.g., 1 USDC)
# WARNING: Only use for testnet! Never put real money in this wallet!
AGENT_PRIVATE_KEY=your_64_character_private_key_here

# Incumbent/API Owner Address (where the API receives payments)
# This is the "real estate company" wallet that receives revenue
# Use a different MetaMask account to show actual payment flow
# Copy address with 0x prefix
RECIPIENT_ADDRESS=0x_incumbent_metamask_address_here
